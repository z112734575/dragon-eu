<!-- Limited-Time Offers -->
<div class="limited_products">
		<div class="limited_products_box">
			{% for product in section.settings.product_list %}
			<div class="limited_products_list">
				<div class="limited_products_list_box">
					<div class="product_img">
						<img src="{{ product.featured_media | img_url: "400x" }}">
					</div>
					<div class="product_title_pire">
						<p class="product_title">{{ product.title  }}</p>
						<div class="product_pire_review">
							{% assign compare_at_price = product.compare_at_price %}
				              {% assign price = product.price %}
				              {% assign showDiscountBar = compare_at_price | minus: price %}
				              {% assign discountbar = product.metafields.custom.discountbar.value %}
				              {% assign discount_sum = discountbar[0] %}
				              {% assign discount_code = discountbar[1] %}
				              {% assign discount_sumM = 100 | minus: discount_sum %}
				              {% if discount_sum %}
							<p class="product_prie">{{price|times: discount_sumM | divided_by:10000.0 | round: 2 }}<span>{{ product.price | money }}</span></p>
							<div class="product_sale"><p>-{{ discount_sum }}<span>%</span></p></div>
							{% elsif showDiscountBar > 1 %}
							<p class="product_prie">{{ product.price | money }}<span>{{ product.compare_at_price | money }}</span></p>
							<div class="product_sale"><p>-{{ compare_at_price | minus: price | times: 100 | divided_by: compare_at_price }}<span>%</span></p></div>
							{% else %}
							<p class="product_prie">{{ product.price | money }}</p>
							{% endif %}
							<div class="product_review">
                              {%- case app_review -%}            
                                  {%- when '1' -%}
                                     <div class="shopify-product-reviews-badge t4s-grid-rating" data-id="{{ product.id }}"></div>
                                  {%- when '2' -%}
                                     <div class="ryviu-collection t4s-grid-rating"><ryviu-widget-total collection=1 reviews_data="{{product.metafields.ryviu.product_reviews_info  | escape  }}" product_id="{{product.id}}" handle="{{product.handle}}"></ryviu-widget-total></div>  
                                  {%- when '3' -%}
                                     <div product-id="{{ product.id }}" class="arv-collection arv-collection--{{ product.id }} t4s-grid-rating"></div>
                                  {%- when '4' -%}
                                     <div class="loox-rating t4s-grid-rating" data-id="{{ product.id }}" data-rating="{{ product.metafields.loox.avg_rating }}" data-raters="{{ product.metafields.loox.num_reviews }}"></div>
                                  {%- when '5' -%}
                                      {%- capture the_snippet_review_avg %}{% render 'ssw-widget-avg-rate-listing', product:product %}{% endcapture -%}
                                      {%- unless the_snippet_review_avg contains 'Liquid error' -%}{{ the_snippet_review_avg }}{%- endunless -%}
                                  {%- when '7' -%}{{ product.metafields.judgeme.badge }}
                                  {%- when '8' -%}
                                    <div class="scm-reviews-rate t4s-grid-rating" data-rate-version2={{ product.metafields.scm_review_importer.reviewsData.reviewCountInfo | json }} data-product-id= {{ product.id }}></div>
                                  {%- else -%}
                                    <div class="t4s-grid-rating t4s-review__grid-other">{%- render 'review_grid_other',product:product -%}</div>
                              {%- endcase -%}
                            </div>
						</div>
					</div>
					<div class="product_code">
						<p>Code: {{ discount_code }} </p>
						<button data-ug-copy-data="{{ discount_code }}" class="copy-btn">Copy</button>
					</div>
					<div class="product_buy">
                        <form method="post" action="/cart/add">
                          <input type="hidden" name="return_to" value="/checkout/" />
                          <input type="hidden" name="id" value ="{{ product.variants.first.id }}" />
                          <input type="submit" value="Buy Now" class="btn btn--primary buybuttonRuta btnmyodrers" />
                        </form>
                         <input type="hidden" name="return_to" value="/checkout/" />
						<a href="{{ product.url }}">Leam more</a>	
					</div>
				</div>
			</div>
			{% endfor %}
		</div>
</div>
<!-- UP To 30% Off -->
<div class="limited_off">
	<h2>{{ section.settings.box_title }}</h2>
	<ul class="limited_off_rights">
		<li class="rights_nav active">Chargers</li>
		<li class="rights_nav">Cables</li>
		<li class="rights_nav">Hubs & Adapters</li>
		<li class="rights_nav">Stands & Protectors</li>
		<li class="rights_nav">Docking Station</li>
		<li class="rights_nav">Earbuds</li>
	</ul>
	<div class="off_products_box" id="ug-section1">
		{% for product in section.settings.product_list1 %}
		<div class="off_product_list">
			<div class="off_list_box">
				<div class="product_img">
					<img src="{{ product.featured_media | img_url: "400x" }}">
				</div>
				<p class="product_title">{{ product.title  }}</p>
					{% assign compare_at_price = product.compare_at_price %}
	              {% assign price = product.price %}
	              {% assign showDiscountBar = compare_at_price | minus: price %}
	              {% assign discountbar = product.metafields.custom.discountbar.value %}
	              {% assign discount_sum = discountbar[0] %}
	              {% assign discount_code = discountbar[2] %}
	              {% assign discount_sumM = 100 | minus: discount_sum %}
	              {% if discount_sum %}
				<p class="product_prie">{{price|times: discount_sumM | divided_by:10000.0 | round: 2 }}<span>{{ product.price | money }}</span></p>
				<div class="product_sale"><p>-{{ discount_sum }}<span>%</span></p></div>
				{% elsif showDiscountBar > 1 %}
				<p class="product_prie">{{ product.price | money }}<span>{{ product.compare_at_price | money }}</span></p>
				<div class="product_sale"><p>-{{ compare_at_price | minus: price | times: 100 | divided_by: compare_at_price }}<span>%</span></p></div>
				{% else %}
				<p class="product_prie">{{ product.price | money }}</p>
				{% endif %}
				<div class="product_code">
					<p>Code: {{ discount_code }} </p>
					<button data-ug-copy-data="{{ discount_code }}" class="copy-btn">Copy</button>
				</div>
				<div class="product_buy">
                        <form method="post" action="/cart/add">
                          <input type="hidden" name="return_to" value="/checkout/" />
                          <input type="hidden" name="id" value ="{{ product.variants.first.id }}" />
                          <input type="submit" value="Buy Now" class="btn btn--primary buybuttonRuta btnmyodrers" />
                        </form>
                         <input type="hidden" name="return_to" value="/checkout/" />
						<a href="{{ product.url }}">Leam more</a>	
					</div>
			</div>
		</div>
		{% endfor %}
	</div>
	<div class="off_products_box" id="ug-section2">
		{% for product in section.settings.product_list2 %}
		<div class="off_product_list">
			<div class="off_list_box">
				<div class="product_img">
					<img src="{{ product.featured_media | img_url: "400x" }}">
				</div>
				<p class="product_title">{{ product.title  }}</p>
					{% assign compare_at_price = product.compare_at_price %}
	              {% assign price = product.price %}
	              {% assign showDiscountBar = compare_at_price | minus: price %}
	              {% assign discountbar = product.metafields.custom.discountbar.value %}
	              {% assign discount_sum = discountbar[0] %}
	              {% assign discount_code = discountbar[2] %}
	              {% assign discount_sumM = 100 | minus: discount_sum %}
	              {% if discount_sum %}
				<p class="product_prie">{{price|times: discount_sumM | divided_by:10000.0 | round: 2 }}<span>{{ product.price | money }}</span></p>
				<div class="product_sale"><p>-{{ discount_sum }}<span>%</span></p></div>
				{% elsif showDiscountBar > 1 %}
				<p class="product_prie">{{ product.price | money }}<span>{{ product.compare_at_price | money }}</span></p>
				<div class="product_sale"><p>-{{ compare_at_price | minus: price | times: 100 | divided_by: compare_at_price }}<span>%</span></p></div>
				{% else %}
				<p class="product_prie">{{ product.price | money }}</p>
				{% endif %}
				<div class="product_code">
					<p>Code: {{ discount_code }} </p>
					<button data-ug-copy-data="{{ discount_code }}" class="copy-btn">Copy</button>
				</div>
				<div class="product_buy">
                        <form method="post" action="/cart/add">
                          <input type="hidden" name="return_to" value="/checkout/" />
                          <input type="hidden" name="id" value ="{{ product.variants.first.id }}" />
                          <input type="submit" value="Buy Now" class="btn btn--primary buybuttonRuta btnmyodrers" />
                        </form>
                         <input type="hidden" name="return_to" value="/checkout/" />
						<a href="{{ product.url }}">Leam more</a>	
					</div>
			</div>
		</div>
		{% endfor %}
	</div>
	<div class="off_products_box" id="ug-section3">
		{% for product in section.settings.product_list3 %}
		<div class="off_product_list">
			<div class="off_list_box">
				<div class="product_img">
					<img src="{{ product.featured_media | img_url: "400x" }}">
				</div>
				<p class="product_title">{{ product.title  }}</p>
					{% assign compare_at_price = product.compare_at_price %}
	              {% assign price = product.price %}
	              {% assign showDiscountBar = compare_at_price | minus: price %}
	              {% assign discountbar = product.metafields.custom.discountbar.value %}
	              {% assign discount_sum = discountbar[0] %}
	              {% assign discount_code = discountbar[2] %}
	              {% assign discount_sumM = 100 | minus: discount_sum %}
	              {% if discount_sum %}
				<p class="product_prie">{{price|times: discount_sumM | divided_by:10000.0 | round: 2 }}<span>{{ product.price | money }}</span></p>
				<div class="product_sale"><p>-{{ discount_sum }}<span>%</span></p></div>
				{% elsif showDiscountBar > 1 %}
				<p class="product_prie">{{ product.price | money }}<span>{{ product.compare_at_price | money }}</span></p>
				<div class="product_sale"><p>-{{ compare_at_price | minus: price | times: 100 | divided_by: compare_at_price }}<span>%</span></p></div>
				{% else %}
				<p class="product_prie">{{ product.price | money }}</p>
				{% endif %}
				<div class="product_code">
					<p>Code: {{ discount_code }} </p>
					<button data-ug-copy-data="{{ discount_code }}" class="copy-btn">Copy</button>
				</div>
				<div class="product_buy">
                        <form method="post" action="/cart/add">
                          <input type="hidden" name="return_to" value="/checkout/" />
                          <input type="hidden" name="id" value ="{{ product.variants.first.id }}" />
                          <input type="submit" value="Buy Now" class="btn btn--primary buybuttonRuta btnmyodrers" />
                        </form>
                         <input type="hidden" name="return_to" value="/checkout/" />
						<a href="{{ product.url }}">Leam more</a>	
					</div>
			</div>
		</div>
		{% endfor %}
	</div>
	<div class="off_products_box" id="ug-section4">
		{% for product in section.settings.product_list4 %}
		<div class="off_product_list">
			<div class="off_list_box">
				<div class="product_img">
					<img src="{{ product.featured_media | img_url: "400x" }}">
				</div>
				<p class="product_title">{{ product.title  }}</p>
					{% assign compare_at_price = product.compare_at_price %}
	              {% assign price = product.price %}
	              {% assign showDiscountBar = compare_at_price | minus: price %}
	              {% assign discountbar = product.metafields.custom.discountbar.value %}
	              {% assign discount_sum = discountbar[0] %}
	              {% assign discount_code = discountbar[2] %}
	              {% assign discount_sumM = 100 | minus: discount_sum %}
	              {% if discount_sum %}
				<p class="product_prie">{{price|times: discount_sumM | divided_by:10000.0 | round: 2 }}<span>{{ product.price | money }}</span></p>
				<div class="product_sale"><p>-{{ discount_sum }}<span>%</span></p></div>
				{% elsif showDiscountBar > 1 %}
				<p class="product_prie">{{ product.price | money }}<span>{{ product.compare_at_price | money }}</span></p>
				<div class="product_sale"><p>-{{ compare_at_price | minus: price | times: 100 | divided_by: compare_at_price }}<span>%</span></p></div>
				{% else %}
				<p class="product_prie">{{ product.price | money }}</p>
				{% endif %}
				<div class="product_code">
					<p>Code: {{ discount_code }} </p>
					<button data-ug-copy-data="{{ discount_code }}" class="copy-btn">Copy</button>
				</div>
				<div class="product_buy">
                        <form method="post" action="/cart/add">
                          <input type="hidden" name="return_to" value="/checkout/" />
                          <input type="hidden" name="id" value ="{{ product.variants.first.id }}" />
                          <input type="submit" value="Buy Now" class="btn btn--primary buybuttonRuta btnmyodrers" />
                        </form>
                         <input type="hidden" name="return_to" value="/checkout/" />
						<a href="{{ product.url }}">Leam more</a>	
					</div>
			</div>
		</div>
		{% endfor %}
	</div>
	<div class="off_products_box" id="ug-section5">
		{% for product in section.settings.product_list5 %}
		<div class="off_product_list">
			<div class="off_list_box">
				<div class="product_img">
					<img src="{{ product.featured_media | img_url: "400x" }}">
				</div>
				<p class="product_title">{{ product.title  }}</p>
					{% assign compare_at_price = product.compare_at_price %}
	              {% assign price = product.price %}
	              {% assign showDiscountBar = compare_at_price | minus: price %}
	              {% assign discountbar = product.metafields.custom.discountbar.value %}
	              {% assign discount_sum = discountbar[0] %}
	              {% assign discount_code = discountbar[2] %}
	              {% assign discount_sumM = 100 | minus: discount_sum %}
	              {% if discount_sum %}
				<p class="product_prie">{{price|times: discount_sumM | divided_by:10000.0 | round: 2 }}<span>{{ product.price | money }}</span></p>
				<div class="product_sale"><p>-{{ discount_sum }}<span>%</span></p></div>
				{% elsif showDiscountBar > 1 %}
				<p class="product_prie">{{ product.price | money }}<span>{{ product.compare_at_price | money }}</span></p>
				<div class="product_sale"><p>-{{ compare_at_price | minus: price | times: 100 | divided_by: compare_at_price }}<span>%</span></p></div>
				{% else %}
				<p class="product_prie">{{ product.price | money }}</p>
				{% endif %}
				<div class="product_code">
					<p>Code: {{ discount_code }} </p>
					<button data-ug-copy-data="{{ discount_code }}" class="copy-btn">Copy</button>
				</div>
				<div class="product_buy">
                        <form method="post" action="/cart/add">
                          <input type="hidden" name="return_to" value="/checkout/" />
                          <input type="hidden" name="id" value ="{{ product.variants.first.id }}" />
                          <input type="submit" value="Buy Now" class="btn btn--primary buybuttonRuta btnmyodrers" />
                        </form>
                         <input type="hidden" name="return_to" value="/checkout/" />
						<a href="{{ product.url }}">Leam more</a>	
					</div>
			</div>
		</div>
		{% endfor %}
	</div>
  <div class="off_products_box" id="ug-section6">
		{% for product in section.settings.product_list6 %}
		<div class="off_product_list">
			<div class="off_list_box">
				<div class="product_img">
					<img src="{{ product.featured_media | img_url: "400x" }}">
				</div>
				<p class="product_title">{{ product.title  }}</p>
					{% assign compare_at_price = product.compare_at_price %}
	              {% assign price = product.price %}
	              {% assign showDiscountBar = compare_at_price | minus: price %}
	              {% assign discountbar = product.metafields.custom.discountbar.value %}
	              {% assign discount_sum = discountbar[0] %}
	              {% assign discount_code = discountbar[2] %}
	              {% assign discount_sumM = 100 | minus: discount_sum %}
	              {% if discount_sum %}
				<p class="product_prie">{{price|times: discount_sumM | divided_by:10000.0 | round: 2 }}<span>{{ product.price | money }}</span></p>
				<div class="product_sale"><p>-{{ discount_sum }}<span>%</span></p></div>
				{% elsif showDiscountBar > 1 %}
				<p class="product_prie">{{ product.price | money }}<span>{{ product.compare_at_price | money }}</span></p>
				<div class="product_sale"><p>-{{ compare_at_price | minus: price | times: 100 | divided_by: compare_at_price }}<span>%</span></p></div>
				{% else %}
				<p class="product_prie">{{ product.price | money }}</p>
				{% endif %}
				<div class="product_code">
					<p>Code: {{ discount_code }} </p>
					<button data-ug-copy-data="{{ discount_code }}" class="copy-btn">Copy</button>
				</div>
				<div class="product_buy">
                        <form method="post" action="/cart/add">
                          <input type="hidden" name="return_to" value="/checkout/" />
                          <input type="hidden" name="id" value ="{{ product.variants.first.id }}" />
                          <input type="submit" value="Buy Now" class="btn btn--primary buybuttonRuta btnmyodrers" />
                        </form>
                         <input type="hidden" name="return_to" value="/checkout/" />
						<a href="{{ product.url }}">Leam more</a>	
					</div>
			</div>
		</div>
		{% endfor %}
	</div>
</div>
<script>
    $(function () {
      if($(window).width() > 768) {
            $(window).scroll(function() {
                var scrollTop = $(window).scrollTop();
                var navTop = $('#ug-section1').offset().top;
                if (scrollTop > (navTop -100)) {
                    $('.limited_off_rights').css({
                        position:'fixed'
                    });
                } else {
                    $('.limited_off_rights').css({
                        position: 'absolute'
                    });
                }
            });
            $(".rights_nav").each((index, item) => {
                $(item).on("click", function () {
                    $(this).addClass("active").siblings().removeClass("active");
                    const top = $(`#ug-section${index + 1}`).offset().top;
                    $("body,html").animate(
                        {
                            scrollTop: top //让body的scrollTop等于pos的top，就实现了滚动
                        },
                        200
                    );
                });
            });
        } else {
            $(".rights_nav").each((index, item) => {
                $(item).on("click", function () {
                    $(this).addClass("active").siblings().removeClass("active");
                    const top = $(`#ug-section${index + 1}`).offset().top - 200;
                    $("body,html").animate(
                        {
                            scrollTop: top //让body的scrollTop等于pos的top，就实现了滚动
                        },
                        200
                    );
                });
            });
        }

        
    })
// copy
    $(function() {
	  let timeId = null;
	  $('[data-ug-copy-data]').click(function () {
	  	clearTimeout(timeId);
	  	timeId = setTimeout(() => {
			var copyText = $(this).attr('data-ug-copy-data');
	  		var input = document.createElement('input');
	  		input.setAttribute('value',copyText);
	  		document.body.appendChild(input);
	  		input.select();
	  		if (document.execCommand('copy')) {
	  			document.execCommand('copy');
	  			$(this).text('Copied');
	  			setTimeout( () => {
	  				$(this).text('Copy');
				},2000)}
				document.body.removeChild(input);
		},200);});
	})
</script>

{% schema %}
  {
    "name": "WeeklySale product list",
    "settings": [
      {
      "type": "product_list",
      "id": "product_list",
      "label": "Limited-Time Offers",
      "limit": 9
    },
      {
      "type": "text",
      "id": "box_title",
      "label": "Title"
    },
    {
      "type": "product_list",
      "id": "product_list1",
      "label": "Chargers Products",
      "limit": 12
    },
      {
      "type": "product_list",
      "id": "product_list2",
      "label": "Cables Products",
      "limit": 12
    },
      {
      "type": "product_list",
      "id": "product_list3",
      "label": "Hubs & Adapters Products",
      "limit": 12
    },
      {
      "type": "product_list",
      "id": "product_list4",
      "label": "Stands & Protectors Products",
      "limit": 12
    },
      {
      "type": "product_list",
      "id": "product_list5",
      "label": "Docking Station Products",
      "limit": 12
    },
      {
      "type": "product_list",
      "id": "product_list6",
      "label": "Earbuds Products",
      "limit": 12
    }
    ],
     "presets": [
    {
      "name": "WeeklySale product list"
    }
  ],
  "templates": ["page"]
  
  }
{% endschema %}

{% stylesheet %}
{% endstylesheet %}

{% javascript %}
{% endjavascript %}